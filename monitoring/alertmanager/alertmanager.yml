global:
  resolve_timeout: 5m

route:
  receiver: "recovery-webhook"

receivers:
  - name: "recovery-webhook"
    webhook_configs:
      - url: "http://host.docker.internal:5001/recover"
        send_resolved: false
